{
  "name": "tafl-project-workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev -- --port 3000\"",
    "build": "npm --prefix frontend run build && npm --prefix backend run build",
    "start": "npm --prefix frontend run start -- --port 3000"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
